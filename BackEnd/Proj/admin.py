from django.contrib import admin
# from .models import Project, Submission, Rubric, Evaluation # Old models

# PynamoDB models do not integrate directly with Django's default admin.
# If you need admin-like functionality for DynamoDB, you would need to implement
# custom views or use a separate tool.
# admin.site.register(Project)
# admin.site.register(Submission)
# admin.site.register(Rubric)
# admin.site.register(Evaluation)

